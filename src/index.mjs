import { countries as c } from './countries.mjs'

import { subdivisions as s } from './subdivisions.mjs'

export const countries = c
export const subdivisions = s
export default {
  countries,
  subdivisions,
}
